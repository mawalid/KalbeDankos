

<table class="table-responsive">
    <tr>
        <td>
            <input type="hidden" name="ID" id="idget" />
        </td>
    </tr>
    <tr>
        <td>
            <span class="glyphicon glyphicon-trash" style="font-size:70px;"></span>
        </td>
        <td align="center">
            <label class="merahtext" style="font-size:17px;">
                &nbsp;&nbsp;&nbsp;&nbsp;Anda yakin ingin menghapus barang <br />
                <input type="text" readonly style="border:0px; width:300px; text-align:center; font-size:20px;" id="idLblNamaBarang" />
            </label>
        </td>
    </tr>
    <tr>
        <td>
            <br />
        </td>
    </tr>
</table>
<div class="row">
    <table align="right">
        <tr>
            <td colspan="1">
                <div class="btn btn-group">
                    <button type="button" class="btn orenwoy" id="idBtnCancelBarang">Tidak</button>
                    <button type="button" class="btn merahwoy" id="idBtnHapusBarang">&nbsp;&nbsp;&nbsp;Ya&nbsp;&nbsp;&nbsp;</button>
                </div>
            </td>
        </tr>
    </table>
</div>

<script>
    $(document).ready(function () {
        $.ajax({
            url: '/Barang/AmbilData',
            data: { ID: $('#idget').val() },
            method: 'Get',
            success: function (mdl) {
                debugger;
                $('#idLblNamaBarang').val(mdl.NamaBarang + ' ?');
            }
        })
    });

    $('#idBtnCancelBarang').click(function () {
        $('#idMdlHapusBarang').close;
        location.reload();
    });

    $('#idBtnHapusBarang').click(function () {
        var vDataID = $('#idget').val();
        debugger;
        //
        //
        $.ajax({
            url: '/Barang/Delete',
            data: { ID: vDataID },
            method: 'Post',
            success: function (mdl) {
                alert('Berhasil Menghapus Barang');
                location.reload();
                debugger;
            },
            error: function (mdl) {
                debugger;
            }
        });
        //
    })
</script>


